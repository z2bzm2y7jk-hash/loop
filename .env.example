# Hostinger stores these values as protected environment variables.
# Never commit a real password or session secret.
DATABASE_URL=mysql://loop_user:replace_me@localhost:3306/loop_golf
AUTH_SECRET=replace_with_at_least_32_random_bytes
APP_ORIGIN=http://localhost:3000
# Optional secure checkout links. Leave blank while all private-beta access is free.
NEXT_PUBLIC_LOOP_CAPTAIN_CHECKOUT_URL=
NEXT_PUBLIC_LOOP_TRIP_CHECKOUT_URL=
